version: "3"

services:
  nginx:
    build: ./src/nginx/
    ports:
      - 80:80
      - 443:443
    environment:
      - TZ=Europe/Moscow
    networks:
      - frontend
    volumes:
      - ./src/frontend/build:/var/www/snek-gc.misha.im/

networks:
  frontend: